syntax = "proto3";

import "google/protobuf/any.proto";
import "client_id.proto";

package protocols;
option go_package=".;protocols";

// 服务->网关->客户端
message Forward {
    network.ClientID   sender = 1;  // 目标(可为空)  
    google.protobuf.Any msg = 2;  // 具体消息 
}
