<UserControl x:Class="Editor.BehaviorCharts.EditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:v="clr-namespace:Bgt.Diagrams;assembly=Bgt.Diagrams"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding LoadedCommand}" CommandParameter="{Binding ElementName=editor}"/>
        </i:EventTrigger>
    </i:Interaction.Triggers>
    <Grid>
        <v:DiagramScrollView>
            <v:DiagramView Name="editor"
                     AllowDrop="True"
                     ShowGrid="True"
                     GridCellSize="20,20"
                     DocumentSize="2000,2000"
                     VerticalAlignment="Bottom"
                     DockPanel.Dock="Bottom">
            </v:DiagramView>
        </v:DiagramScrollView>
    </Grid>
</UserControl>
