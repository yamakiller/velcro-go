<UserControl x:Class="Editor.Panels.PanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:dg="clr-namespace:Behavior.Diagrams;assembly=Behavior.Diagrams"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding LoadedCommand}" CommandParameter="{Binding ElementName=editor}"/>
        </i:EventTrigger>
    </i:Interaction.Triggers>
    <Grid>
        <dg:DiagramScrollView>
            <dg:DiagramView Name="editor"
                                 AllowDrop="True"
                                 ShowGrid="True"
                                 GridCellSize="20,20"
                                 DocumentSize="1000,2000"
                                 VerticalAlignment="Bottom"
                                 DockPanel.Dock="Bottom"/>

        </dg:DiagramScrollView>
    </Grid>
</UserControl>
